/** @type {import('next').NextConfig} */

// const nextConfig = {};

const nextConfig = {
    // async rewrites() {
    //     return [
    //         {
    //             source: '/api/:path*', // Exclude NextAuth API routes
    //             destination: '/api/:path*',
    //         },
    //         {
    //             source: '/:path*', // Catch-all for other routes
    //             destination: '/',
    //         },
    //     ];
    // },
};

export default nextConfig;
